# Review — 0.4.0/the-surface-says-telemetry + 0.4.0/recording-is-on-or-the-gate-is-red

Two features, one record: they are the same argument at two layers — the word in the surface, and
the gate that says the surface is lying. Commits `c467642` (A) and `56d80ee` (B), read at tree
state `715a940`. Reviewed cold against both `feature.md`s, both stories including their close
blocks, `decisions.md` D1/D3/D5/D6/D7/D8, `CLAUDE.md` hard rules 4/5/6/9/10/11, and the three prior
records `2026-09-06-0.4.0-one-rule-routes-a-row.md`, `2026-09-06-0.4.0-every-row-names-its-grain.md`
and `2026-09-06-the-code-knows-entry-and-exit.md`, so nothing already dispositioned is re-raised.
Refs tool: raw `grep` plus live CLI probes against a scratch checkout — this repo ships no
reference-aware search.

**Verdict: SHIP-WITH-FIXES.** Both theses hold. A's `grep -ri telemetry` now lands on four files in
`src/` and three of them are discovery surfaces, and it added no capability — the diff is a usage
string, one guidance markdown and tests. B's D11 is genuinely a WARN: I could not make it move an
exit code in any shape, the opt-out is right in all three forms, and `pm-execution.md` step 1's new
clause is TRUE of the shipped code — no write path consults a status. What is wrong is one branch
that can silence the whole rule without saying so, and one test that claims to prove a ship
criterion it cannot fail on.

## Gates, run here

Clean `git archive 715a940` into scratch, `git init` + one commit (the shell check reads
`git ls-files`), `bash tools/setup-hooks.sh` armed. Nothing below rests on the moving worktree.

| gate | result |
|---|---|
| `make precommit` | `[CHECK] 5 check(s) PASS`; `[UNIT] 785 passed, 3 skipped, 393 deselected, 409 subtests` in 10.83s — exit 0 |
| `make test` (feature gate) | `[TEST] 1174 passed, 7 skipped, 463 subtests` in 108.21s — exit 0 |

Twelve hand-built probe trees driven through `agentic_sdlc.cli.main(['check','pm'])` in-process;
every claim below marked *probed* came out of one of them, not out of reading the diff.

## Blockers

- **`M1` MAJOR `src/agentic_sdlc/repo/checks/pm.py:291-310`** — `_tree_has_a_row` returns `True` on
  `OSError`/`UnicodeDecodeError`, and returns it on the FIRST path that raises, so the remaining
  ledgers are never consulted. One unreadable file silences D11 for the entire tree, with **no line
  at all** — not a WARN, not the UNVERIFIABLE the sibling branch prints five lines above. Probed:
  couriers wired, zero rows anywhere, `pm/roadmap/ledger.jsonl` empty and `chmod 000` — the exact
  artifact the docstring names as "what a courier leaves when it created the file and refused" —
  gives `[check:pm] PASS — no PM-tree status drift; scanned 1 milestone(s), 1 feature(s),
  1 story/ies, 0 bug(s)` with no warning count. The same tree with that file readable gives the
  WARN. **The rule-4 citation at :306-308 is backwards**: rule 4's first sin is *a gate that misses
  drift and prints PASS*, which is what `True` produces here; `False` would over-report, which is
  the opposite failure and costs nobody an exit code. Root-owned files out of a container run, a
  courier killed mid-append (`ledger.py:217 _ends_mid_line` exists because torn tails are already
  an acknowledged reality), or a foreign-owned checkout all reach it. No test covers the branch —
  the six D11 cases at `tests/test_pm_gate.py:717-816` never make a ledger unreadable. Fix: `warn`
  UNVERIFIABLE naming the path, and `continue` the loop instead of returning, so one bad file does
  not answer for the others.
- **`M2` MAJOR `tests/test_cli_surface.py:186-190`** — `test_this_feature_added_no_verb_and_no_flag`
  asserts `documented_verbs() == routed_verbs()`. That is exactly the conjunction of
  `test_every_documented_verb_is_routed` (:58) and `test_every_routed_verb_is_documented` (:65),
  which already assert both inclusions, so it is a third assertion of a standing invariant and adds
  no failure mode. Worse, it cannot fail for what it claims: it would pass unchanged if this feature
  had added a verb **and documented it**. Story 01's proof table says criterion 5 is `existing,
  unamended` — the case should not have been written at all (rule 10, prove it once) — and the close
  block states *"AC5 held: `test_cli_surface.py` asserts the routed verb set is unchanged"*, which
  the file does not assert. AC5 is in fact TRUE: `c467642` touches `pm/cli.py` only inside `USAGE`
  (hunk `@@ -179,14 +179,21 @@`, and `USAGE` runs `cli.py:21` to `:2066`), plus one guidance
  markdown and two test modules — no route, no flag, no config key, no module. The defect is the
  stated proof, not the code. Fix: delete the redundant case and correct the close line, or pin the
  roster as a frozen literal if "unchanged" is what is wanted.

## Findings

- **`W1` WARNING `src/agentic_sdlc/repo/checks/pm.py:5`** — the gate's own contract line still reads
  *"(default: D1-D6 + V1-V5; V6, D7, D9/D10 and the R family are opt-in)"*. D11 is opt-in
  (`model.py:138`) and is not in it. It reached the WARN roster at :24 and not the roster line a
  consumer reads to learn what they may switch on. Rule 11's own miss, in the file that added D11.
- **`W2` WARNING `src/agentic_sdlc/repo/installables/project-devkit.toml:76-84`** — the config
  `init` seeds into a consumer enumerates the opt-ins in its `checks` comment — V6, D9, D10, R5 —
  and never names D11. This repo's `README.md:175-178` got the line; the file a consumer actually
  edits did not. Same class as the feature: the capability is absent from the surface someone is
  standing in when they choose.
- **`W3` WARNING `src/agentic_sdlc/repo/checks/pm.py:359`** — the second of the four named causes,
  *"`[pm.states.*]` is undeclared, so every work-moving verb refuses"*, can never be the cause of a
  D11 line. An undeclared flow raises `model.ConfigError` out of `_drift_walk`, which runs before
  `_recording_findings` (:110-112), and `run()` exits 2. Probed: `checks = ["D11"]` with no
  `[pm.states.*]` gives `[check:pm] ERROR — this tree declares no flow …` at exit 2 and no D11 line
  ever. A differential diagnosis listing an already-excluded cause spends the reader's attention on
  the one thing the line exists to save. Fix: drop it, or say that seeing this line rules it out.
- **`W4` WARNING `src/agentic_sdlc/repo/checks/pm.py:348`** — `wired` is a bare substring test over
  the whole settings text, and the line it produces asserts *"this tree is recording NOTHING"*.
  Probed: a `settings.json` whose only mention is
  `{"permissions":{"allow":["Bash(bash tools/hooks/cc-ledger-session.sh --self-test)"]}}` — the
  allowlist entry `install-hooks`' own next-step text tells consumers to create
  (`install.py:169-172`) — produces the full WARN with no hook registered anywhere.
  Over-reporting is the safe direction; the wrong direction is also reachable: probed, couriers
  pasted into `.claude/settings.local.json` (the per-user override Claude Code writes itself, and
  gitignored) are invisible and the tree is silent. Fix: parse the JSON and look under `hooks`,
  consult both files, and say which file was read.
- **`W5` WARNING `56d80ee`** — the commit adds `docs/reviews/2026-09-06-0.4.0-every-row-names-its-grain.md`
  (234 lines), a review record for a **different** feature, inside feature B's `feat(...)` commit.
  Confirmed with `git show --diff-filter=A --name-only 56d80ee`. The reviewer contract commits a
  record pathspec-limited; that record now has no commit of its own and `git log --` on it names
  feature B. Nothing functional, but the log no longer says what the feature touched.
- **`W6` WARNING `src/agentic_sdlc/repo/pm/guidance/pm-operations.md:17-42`** — the new 26-line body
  section restates `pm --help`'s two ledger blocks (`cli.py:182-199`) and `pm-execution.md`'s two
  entries (:146, :158) nearly clause for clause: the columns, `--json`, `--from <rev>`, the two
  homes. 0.4.0/D6's rule is *"a document carries only what nothing else holds; everything derivable
  is a command it names"*, and in this same milestone review rejected a handoff skill that restated
  its template instead of pointing at it. AC3 is about the `description:`, which needs no body
  beyond a pointer to keep its promise.

## Suggestions and deltas

- **`S1` SUGGESTION `src/agentic_sdlc/repo/checks/pm.py:353-362`** — the WARN renders as one
  **625-character** line. The next longest finding string in the module is 287 and the family median
  is ~140. It is also the only gate output string anywhere in `src/agentic_sdlc/repo/checks/` that
  carries literal `**…**`, which prints as asterisks in a terminal. Hard rule 6 makes these shapes
  a consumer-facing contract; wrap it or split the causes onto their own indented lines.
- **`S2` SUGGESTION** — extending `pm-operations` beat shipping a third skill, and the call is
  right: rule 11 says fix at the cheapest layer, the words only have to be where a selector reads,
  and a third skill would be D6's "fourth name for a fact" with `pm --help`, `pm-execution.md` and
  the README already holding three. But the skill is now 175 lines and a 627-char description
  against 72/91 lines and 316/330 chars for `handoff` and `release` — seven sections spanning
  grains, vocabularies, decomposition, phases, tool-reading, retirement and now telemetry. Nothing
  caps a skill's description or body, while `[grain_shape] caps` bounds every grain doc, and D6's
  own finding was that a repeated cap failure is the cheapest detector of a document restating
  things. The detector that caught the handoff does not exist for the surface that absorbed this.
- **`S3` SUGGESTION** — the check-rule namespace and the decision namespace now both hold a `D11`:
  0.2.0's decision D11 (*a callee's exit 2 is UNVERIFIABLE*), live in `conveyor/driver.py:10,120,210,626`
  and `conveyor/steps.py:239,248,974`, and this rule. `grep -rn D11` over `src/` returns both
  families interleaved. 0.4.0/D3 already flags the same collision for D6 in prose; one line where
  the rule ids are listed would settle it for the next reader.
- **`D1` DELTA `CHANGELOG.md:42-62`** — the entry states the empty-file rule and the UNVERIFIABLE
  settings rule, and omits the third: an unreadable LEDGER answers "has rows" and silences the rule.
  Consumer-visible behaviour, and if `M1` lands the note changes with it. For the tech-writer.

## The criteria — A, `0.4.0/the-surface-says-telemetry`

| # | criterion | verdict | shown by |
|---|---|---|---|
| 1 | `pm --help`'s ledger lines say what they ARE | met | `src/agentic_sdlc/repo/pm/cli.py:182-199`; `tests/test_cli_surface.py:173` |
| 2 | both ledger verbs in `pm-execution.md`'s read-verb list | met | `guidance/pm-execution.md:146` and `:158`, both inside § "Keeping the tree honest" (:132, the last section) |
| 3 | a shipped skill's `description:` carries the vocabulary | met | `guidance/pm-operations.md:3`; asserted post-`install-skills` at `tests/test_pm_guidance.py:76-97` |
| 4 | the class stated ONCE in `CLAUDE.md` | met | `CLAUDE.md:71`, one occurrence; landed `05c8364` as rule 11's read side, as the close says |
| 5 | no new verb, flag, config key or capability | met, proof inert | true by the diff of `c467642` (USAGE prose + one guidance md + tests); **not** shown by the case that claims it — see `M2` |
| — | ship criterion: `grep -ri telemetry` hits a discovery surface | met | `grep -ril telemetry src/` → `pm/cli.py`, `guidance/pm-execution.md`, `guidance/pm-operations.md`, `checks/pm.py`; three of the four are discovery surfaces and two of those ship to consumers through `install-skills` |

## The criteria — B, `0.4.0/recording-is-on-or-the-gate-is-red`

| # | criterion | verdict | shown by |
|---|---|---|---|
| 1 | next free `D` id, a WARN never the exit code, opt-in, in `KNOWN_CHECKS` + README + CHANGELOG | met, with `W1`/`W2` | `model.py:138` (`USAGE_CHECKS`) feeding `KNOWN_CHECKS` at `:154-157`; `README.md:175-178`; `CHANGELOG.md:42`; `pm vocabulary` prints `… D7 D11 D9 D10 …` on its `rules` line. Absent from `checks/pm.py:5` and from `project-devkit.toml` |
| 2 | the line names the four causes and the ONE command | met, with `W3` | `checks/pm.py:353-362`; probed output names `.PHONY`, `[pm.states.*]`, `python3`/transcript, a missing script, and `--self-test`. One cause is unreachable |
| 3 | wired+rows silent, wired+no rows WARN, unwired silent | met | probed all three, plus a fourth: no `settings.json` at all → PASS, 0 warnings; `{}` → PASS, 0 warnings; wired + a row in **either** home → PASS, 0 warnings; wired + no rows → the WARN at exit 0 |
| 4 | an unparseable `settings.json` is UNVERIFIABLE, never a failure | met | `checks/pm.py:341-346`; probed with `\xff\xfe … \x00` → `WARN … UNVERIFIABLE — not a finding, and not a pass either (D11)`, exit 0 |
| 5 | `pm-execution.md` step 1 no longer implies the flip starts recording | met, and the claim is TRUE | `guidance/pm-execution.md:69-74`. Traced: `_row_ledger_dir` (`pm/cli.py:1447-1481`) reads only `milestone_dir_of`; `_stamp` (`:330`) likewise; `model.in_progress_milestones` has exactly two callers in `src/` — `release_ledger_dir` (`model.py:1438`), which `_report_default_dir` (`cli.py:2022-2034`) uses to pick which milestone a bare `ledger report` is ABOUT, and `checks/pm.py:367` for D9/D10. **No write path consults a status.** `_building_ledger_dir` survives only as a retired name in `tests/test_boundaries.py:466` |
| 6 | no hook's fail-open behaviour changes | met | `56d80ee` touches no file under `installables/` or `tools/hooks/`; `tests/test_hooks_payloads.py` unchanged and green in the `make test` run above |
| — | D11 can never redden a tree | met | `_recording_findings` calls only `warn`, and `_verdict` (`checks/pm.py:538-561`) computes its code from `findings` alone. Probed with D11 as the only finding → exit 0; probed with D11 beside a real D1 DRIFT → `FAIL — 1 status-drift violation(s) … ; 1 warning(s)`, exit 1 from the drift, D11 counted separately |
| — | stock defaults unchanged (hard rule 5) | met | `DEFAULT_CHECKS` (`model.py:130`) excludes D11; probed a wired, row-less tree declaring flow and no `checks` key → no D11 line |

## The six things I was asked to attack

1. **Every landing site for a new rule.** Module edit, `KNOWN_CHECKS` via `USAGE_CHECKS`, README
   comment, CHANGELOG line, `devkit.toml` roster and `pm vocabulary` are all there. Two are not:
   `check pm --help`'s opt-in roster line (`W1`) and the consumer's seeded `project-devkit.toml`
   (`W2`). D11 is not in `RETIRED_CHECKS` and does not belong there.
2. **It cannot redden.** Proven structurally and probed both ways; the opt-out is right in all three
   shapes. See the last two rows of B's table.
3. **`_tree_has_a_row` is the wrong way round.** `M1`, with the hiding case constructed and run.
4. **A added no capability, and the grep now lands.** Both confirmed; the proof of the first is
   inert (`M2`).
5. **Is `pm-operations` a grab-bag?** Not yet, and extending beat a third skill for the reason D6
   gives. But it is the widest skill by 2x on both axes with no cap on either — `S2`, and the body
   duplication is `W6`.
6. **"The flip is bookkeeping, not the switch."** TRUE at this commit; traced above, criterion 5.

## What I could NOT verify

- **`install.py:181`'s claim that `agentic-sdlc adopt` reports an unwired courier.** It landed in
  `4c9c197`, outside both commits under review, and `telemetry-arrives-with-the-bump` owns it. I did
  not run `adopt` against an unwired tree.
- **Whether a real `Stop`-hook kill produces an undecodable `ledger.jsonl`.** `M1`'s probe used
  permissions, which is sufficient to demonstrate the branch; the torn-write route is argued from
  `ledger.py:217`'s own docstring, not measured.
- **Skill selection behaviour.** Whether the extended `description:` actually wins selection against
  `handoff` and `release` for "how long did this take" is not testable from this repo; only the
  string is asserted.
- **Consumer-side effects of the `pm --help` text change.** Hard rule 6 makes output shapes a
  contract; I confirmed no line shape a gate emits changed, but no consumer tree was run against it.


## Landed — the orchestrator's pass

Under 0.3.0's severity rule (SDLC.md § 0): **BLOCKER/CRITICAL/MAJOR block a close; everything
below is recorded, named here, and CARRIED.** Both MAJORs landed in `PENDING`. The warnings and
suggestions below are real and are carried deliberately — this milestone is being built to a clock,
and a NIT holding a feature is what teaches reviewers to stop writing NITs.

- **M1 landed** — `_tree_has_a_row` returned `True` on the first unreadable ledger, silencing U2 for
  the whole tree with no line printed, over exactly the artifact its own docstring names. It now
  returns `(found, unreadable)`; an unreadable ledger is reported UNVERIFIABLE and the scan
  continues. The rule-4 citation was backwards and says so now.
- **M2 landed** — the AC5 guard asserted `documented == routed`, the conjunction of two cases
  already in the file, and would have passed a verb added AND documented. It asserts the routed
  COUNT, which is what the criterion claims. The story's close is corrected: it said the case
  asserted something it did not.
- **CARRIED**: W1-W6, S1-S3, D1. Worth naming: `check pm --help`'s opt-in roster line and the
  seeded `project-devkit.toml` do not list U2 yet; `wired` is a bare substring test, so the
  allowlist entry `install-hooks` itself prints produces a false WARN and
  `.claude/settings.local.json` is invisible; one of U2's four named causes is structurally
  unreachable because that tree exits 2 first. The last is the sharpest and belongs in the
  milestone review.

```text
verdict: SHIP-WITH-FIXES
| id | severity | disposition |
| M1 | MAJOR | landed PENDING |
| M2 | MAJOR | landed PENDING |
| W1 | WARNING | deferred: 0.5.0 |
| W2 | WARNING | deferred: 0.5.0 |
| W3 | WARNING | deferred: 0.5.0 |
| W4 | WARNING | deferred: 0.5.0 |
| W5 | WARNING | deferred: 0.5.0 |
| W6 | WARNING | deferred: 0.5.0 |
| S1 | SUGGESTION | deferred: 0.5.0 |
| S2 | SUGGESTION | deferred: 0.5.0 |
| S3 | SUGGESTION | deferred: 0.5.0 |
| D1 | DELTA | deferred: 0.5.0 |
```
