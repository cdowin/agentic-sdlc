# Review — 0.6.0/the-changelog-is-a-field-and-a-verb

Range: the feature's changeset in the working tree on top of `427dc70` —
`src/agentic_sdlc/repo/pm/changelog.py` (new), `changelog:` in all four grain templates, the
top-level verb route in `src/agentic_sdlc/cli.py`, `_changelog_answered` (D12) in
`src/agentic_sdlc/repo/checks/pm.py`, `check_changelog_unreleased_nonempty` in
`src/agentic_sdlc/repo/conveyor/steps.py`, `tests/test_pm_changelog.py` (new, 8 cases), the
retirement note in `tests/test_install.py` and the frozen `CHANGELOG.md`.

Solo build, self-reviewed; the disposition on the `reviewing` arrival says so.

**Verdict: SHIP.**

```
verdict: SHIP
| id | severity | disposition |
| C1 | MAJOR | landed in-place |
| C2 | WARNING | landed in-place |
| C3 | WARNING | landed in-place |
| C4 | SUGGESTION | landed in-place |
```

  * **C1 (MAJOR).** The ship criterion says `CHANGELOG.md` is *"retired from the installables and
    from this repo's own tree"*, and two sentences earlier says *"Shipped releases keep whatever
    `CHANGELOG.md` recorded."* Those pull opposite ways on one file, so it was raised rather than
    inferred. **Chris ruled: delete it.** The file is gone; git history keeps the four shipped
    releases' notes, the retirement is named in `test_install.py`'s sweep, and `[release] changelog`
    — the config key naming the file — is refused at exit 2 telling a consumer where it went.
    History is NOT backfilled onto the 168 already-closed grains: the feature rules that out, and
    D12 excludes shipped milestones for the same reason.
  * **C2** — D12 warned on all 168 historically-closed grains before it was narrowed to unshipped
    milestones. That is the 351-of-359 unactionable-warning defect this milestone exists to fix,
    committed by this milestone's own rule; `test_D12_is_silent_over_a_SHIPPED_milestone` holds it.
  * **C3** — the release check's PASS line called every unanswered grain `declined`, which was the
    arithmetic remainder rather than what any grain said. Rule 4 in a census line.
  * **C4** — the collector was named `walk()` and `test_boundaries.py::OneWalk` flagged it as
    filesystem enumeration on the name alone. Renamed `collect()` rather than exempted.

## What was probed, and what bit

| probe | result |
|---|---|
| a milestone whose `order:` interleaves bugs and features | **BIT.** Walking kind-by-kind reorders the notes away from what shipped when. `_children` reads `order:` WHOLE across kinds; the case asserts both orderings of the same two ids. |
| `[pm.contains]` narrowed | Not used — `BINDS_TO` is, for D11's reason: a narrowed mapping would drop a level's entries in silence. |
| `none` vs empty | Both exercised in both gates. `none` renders nothing and satisfies; empty is named. |
| a cycle in the bindings | `seen` guards it. Read, not tested — named below. |
| D12 over a shipped milestone | Silent, and the census says `0 of 0` rather than nothing at all. |

## Migration performed

Six `## Unreleased` bullets condensed to four grain sentences — one per grain, which is the
discipline the field imposes: three of the six bullets were all `bg-a-bug-is-a-grain-nested-in-a-parent`
and are now one sentence, with the long-form rationale where it already lived (the grain body and
the decision log). `agentic-sdlc changelog ms-the-rule-reaches-the-work` renders all four in
`order:` sequence.

## What I did NOT verify

- **No second reviewer.**
- **The cycle guard (`seen`) has no case.** A binding cycle is a V7 finding upstream, so this is
  belt-and-braces — but it is untested belt-and-braces.
- **`--from <rev>`, named in the feature's shape line, is NOT implemented.** The verb takes a grain
  id or defaults to the current release. Nothing in the ship criterion asks for `--from`, so this is
  a narrowing rather than a miss — named so it is not discovered later as a surprise.
- **No consumer tree.** The retirement is named in the sweep (`test_install.py`), which is what tells
  a consumer bumping the pin where the file went; I did not run an actual adopt.
- `make test`, `matrix` and `budget` were not run for this feature alone — the working tree
  currently holds two other agents' in-flight work, so an integration-tier result would not be
  attributable. `make check` and `make unit` are green.
