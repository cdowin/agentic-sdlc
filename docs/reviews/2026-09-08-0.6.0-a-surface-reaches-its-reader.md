# Review — 0.6.0/a-surface-reaches-its-reader-or-it-is-decoration

Range: `799adfa`, `aaede29`, `7290c0d`, `5d7359a`, `7b6a13c` — 41 distinct files, +1227/-126. Twelve
shipped agent definitions gain a `role-verbs` block and `tests/test_install.py` gains the resolver
that holds them to the router; `pm/cli.py`'s dispatch table is hoisted to a public `commands()`;
`tests/test_cli_surface.py`'s router reader becomes a graded classifier with its own violation
corpus; `check hooks` gains the `REGISTERED` line and `git-armed at <path>` (D6); the prose census
reclassifies printed module docstrings as code (D7); `CLAUDE.md` goes through the four questions and
one wrong path is fixed. The grain file's own "What landed" text is read here as the claim under
review, not as changeset.

The working tree is shared with at least one other agent mid-edit — `src/agentic_sdlc/repo/pm/remote.py`
carried a foreign `_planted` probe at 21:50:18 and was reverted by its own author at 21:50:26, while
this review was running. Every plant below was made against a `cp` backup under the scratchpad and
restored immediately; `git status` was asserted clean after each. Nothing in this checkout was
written by me except this file.

**Verdict: SHIP-WITH-FIXES.**

```
verdict: SHIP-WITH-FIXES
| id | severity | disposition |
| S1 | MAJOR | landed in-place |
| S2 | MINOR | open |
| S3 | MINOR | open |
| S4 | MINOR | landed in-place |
| S5 | MINOR | open |
| S6 | MINOR | landed in-place |
| S7 | MINOR | landed in-place |
| S8 | WARNING | landed in-place |
| S9 | WARNING | open |
| S10 | NIT | landed in-place |
| S11 | NIT | landed in-place |
```

The two questions a feature review asks, answered first.

**Does this feature do what its criterion says?** Three of the four criteria, yes, and criterion 1 is
the strongest half of the change. The resolver is real: 7 of 7 planted unroutable citations went red
naming file, line and verb, across all seven roster positions, and 11 of 11 planted arguments — ids,
project-declared state words, flags, placeholders — stayed green. An over-eager resolver that
reddened on `pm story building st-a-thing` would have been as bad as one that missed a verb, and it
does not. Criterion 2 and 3 land as described, and criterion 3's measurement I reproduced
independently in a third session (below). **Criterion 4 is claimed met by a line that cannot fail**,
which is S1 and the only thing blocking.

**Does it commit either of rule 4's sins?** Not the second — no write in this changeset is anything
other than what it says. On the first, the honest answer is *not today, and the margin is thinner
than the record says*: the prose census's new exclusion is genuinely derived and the ceiling still
bites on ordinary comments, but the exclusion is attributed by text match rather than by provenance
(S2) and it now *pays* headroom for printed prose at 1 comment line per 3 docstring lines, with the
guard meant to stop that set roughly ten times too loose (S3). Neither is a gate lying now. Both are
a gate that can be made to miss later, cheaply, by a maintainer under exactly the pressure that
produced D7.

**On D7 specifically, because the contract asks for a verdict and offers MAJOR: I do not think this
was a rule turned off to make today green, and I would say so if I did.** Three things decide it.
The exclusion is DERIVED, not a roster — withdrawing `print((module.__doc__ or '').strip())` from
the check dispatcher drops it from 9 modules and 214 lines to 2 and 71, exactly as claimed, so it
tracks the code and cannot drift toward a flattering number. The ceiling is unchanged and still
bites — 277 plain comment lines lands the census at 0.33336 and red, exactly as claimed. And the
consistency argument is not rhetorical: **587 lines of help text already sit in `USAGE`-style string
constants in `src/` and are counted as CODE** (`pm/cli.py`'s `USAGE` alone is 387 lines), against
214 lines of the identical kind of text in docstrings counted as prose. Grading 587 one way and 214
the other is a category error in the measurement, and correcting it in the direction that makes the
two spellings agree is a correction. The findings below are about the exclusion's *shape*, not about
whether it should exist.

  * **S1 (MAJOR).** Criterion 4 is *"At least one repeated correction from this milestone is
    converted from prose into a gate."* The grain attributes it to the `check hooks` work and says
    it "Landed as the REGISTERED line and `git-armed at <path>` (D6)". **The REGISTERED line cannot
    fail.** `_arming()` is printed unconditionally and contributes nothing to `findings`; I confirmed
    the exit code is 0 in every one of the four new cases, including the one where NONE of the five
    `cc-*` hooks is registered anywhere. D6 rejects making it fail, and that reasoning is sound — a
    tree that registers nothing has opted out, and a registered hook is not a running one either.
    But the consequence is that criterion 4's stated deliverable is not a gate, in the milestone
    whose own northstar sentence — quoted in `7290c0d`'s commit message — is *a gate which cannot
    fail is not a gate*. The correction it names was never prose-only either: `cc-commit-pathspec.sh`
    already existed. So "All four ship criteria are met" is not true as written. **The remedy is
    cheap and I would take the first of these:** re-attribute criterion 4 to
    `test_every_verb_an_agent_definition_names_resolves_against_the_cli`, which IS a repeated
    correction (six briefs this milestone hand-pasted the verb roster) converted into a gate that
    demonstrably fails — 7 of 7 probes red, naming file, line and verb; or scope criterion 4 in
    writing the way sweep 4 was scoped, which the grain already does well. Either way this is a
    record-accuracy defect over sound work, not a code defect, and it should cost one paragraph.
  * **S2 (MINOR).** `tests/test_prose_census.py::published_docstrings` decides the exclusion with
    `doc.strip() in text` over the printed corpus — **text identity, not provenance**. I planted
    `src/agentic_sdlc/repo/_probe_unprinted.py`, a module nothing routes, nothing imports and
    nothing prints, whose docstring was a copy of `checks/shell.py`'s; it was excluded. The
    predicate cannot tell "this module's `__doc__` was printed" from "this text appears in some
    help". The printed corpus holds 766 distinct non-blank lines, and any one of them used verbatim
    as a module docstring is an exclusion — `'Static gates'` already collides. The blast radius is
    small today (only whole-docstring matches, and short docstrings are few lines) and the fix is
    cheap: attribute by object identity — walk the surfaces, capture the `module.__doc__` objects
    actually printed, and exclude those modules — rather than by substring.
  * **S3 (MINOR).** The exclusion does not merely stop charging for printed docstrings, it **pays
    headroom for them**. In `census()`, a published line is subtracted from `prose` and added to
    `code`, so every 3 lines of essay written into a printed docstring buys 1 extra line of ordinary
    comment anywhere in `src/`. Probed: 277 lines of filler inside `checks/shell.py`'s module
    docstring passes green, takes the ungraded ratio to 0.35384, and moves headroom from 276.7 prose
    lines to **369.0** — the same 277 lines as plain comments is red. The rule-4 guard written to
    catch exactly this, `assert moved < prose`, is set at 50% against an actual value of 4.8%
    (214/4460), so it has roughly **4,246 lines of slack** before it fires, and because `moved`
    grows while `prose` stays flat it is the wrong axis besides. Suggest bounding `moved` against
    `code` near today's value (`moved < code // 20` is ~710 lines, ample and actually a tripwire).
    Nine modules publish today and seven are check modules, so the channel widens with every new
    gate.
  * **S4 (MINOR).** The resolver stops one position short of a real sub-roster, so the ship
    criterion's *"a test asserts each named verb EXISTS"* has a hole with a shipped citation in it.
    `_verb_rosters()` wires seven positions and `pm ledger` is not one, so `report` in
    `architect.md`'s `agentic-sdlc pm ledger report <milestone-id>` is graded as an argument. I
    planted `pm ledger frobnicate`, `pm config frobnicate`, `pm templates frobnicate`,
    `pm roadmap frobnicate` and `pm next frobnicate` — all five passed silently. 24 of the 71
    citations have at least one token past the graded depth; most are genuinely arguments (ids,
    flags, `pm story building`'s project-declared state word, correctly ungraded per rule 8), but
    `pm ledger {record,show,report}` is a real verb roster. The fix is the move this feature already
    made once: `pm/cli.py:2133` currently spells that roster as a literal `'(record, show, report)'`
    inside a `Usage` string beside the if/elif chain that implements it — the same second-scoreboard
    shape `commands()` was hoisted to fix. Hoist it and add the eighth position.
  * **S5 (MINOR).** **The router-reader restructure is NOT census-gaming, and I probed it both ways
    before saying so** — but it sits in an unnamed hole in `tests/test_guard_corpus.py` that anyone
    can now use. The honest half: planting the silent drop (`return set(), []`) in `_router_branches`
    turns the replay red and names both shapes that route through it, so the classifier's real
    failure mode is caught, once, in the class that owns it. The hole: `_guards()` computes
    `shaped = _parses(node) or (_named(node) & readers)`, and `_readers()` only walks module-level
    `FunctionDef`s — so binding the classifier's result to the module-level values `_ROUTER_VERBS` /
    `_ROUTER_UNREAD` breaks the name-chain and takes four consumer classes out of the population.
    Measured: re-inlining `_router_branches` into `routed_verbs()` takes `test_cli_surface.py` from
    **1 shaped guard to 5**, the four new ones all uncovered. The commit's stated reason — four
    copies of one corpus for a reader none of them implements — is right, and demanding four
    UNCOVERED entries for a reader that IS covered would have been worse. But nothing in
    `test_guard_corpus.py` names module-level binding as an exit from its census, and this changeset
    is the first use of it. Suggest extending `_readers()` so a module-level assignment whose value
    calls a reader makes the assigned names readers.
  * **S6 (MINOR).** Criterion 2's punchline number is wrong in the direction that flatters it. The
    grain says **"The line-count outcome is -1"**. `CLAUDE.md` is 169 lines at `799adfa^` and **171**
    at HEAD; `7b6a13c`, the four-questions commit itself, is 6 insertions and 6 deletions for a net
    of **0**, and the +2 comes from `7290c0d`'s path fix. So the outcome is 0 for the pass and +2 for
    the feature, not -1. The paragraph's argument survives the correction intact — the point is that
    each cut removes a line somebody has to edit, not that the file shrank — but the number as
    printed is contradicted by the tree, in the section that exists to report a number.
  * **S7 (MINOR).** "Two definitions got SHORTER" is false as written. `pm-operator.md` is +15/-6 for
    a net of **+9** and `tech-writer.md` is +12/-3 for a net of **+9**; all twelve installables grew,
    by +7 to +14. What is true and worth keeping is that those two are the only ones with deletions
    — prose was removed, not just added. Reword to that.
  * **S8 (WARNING).** D6's scoping is proven against one wrong reader and not the other, and the
    docstring is honest that this is so. I confirmed both halves:
    `test_an_allowlist_entry_naming_a_hook_is_not_a_registration` goes red when `settings_commands`
    is replaced with `path.read_text()`, and stays green when it is replaced with a whole-document
    structural walk (`_commands(data)` instead of `_commands(data['hooks'])`). Since `_commands` is
    recursive over any dict or list, a future refactor to the unscoped walk passes every case in the
    suite — and `.claude/settings.json` really does hold `{command: ...}` nodes outside `hooks`
    (`statusLine`, `mcpServers`). One more case with such a node naming a hook, and no `hooks` key,
    would close it.
  * **S9 (WARNING).** Observed during this review rather than in the diff, and it bears directly on
    S1: a concurrent agent wrote `src/agentic_sdlc/repo/pm/remote.py` in this shared working tree at
    21:50:18 and reverted it at 21:50:26. It cleaned up after itself and nothing is left behind. It
    is live evidence that the state D6 measured is still the state — multiple agents planting probes
    in one worktree while the guard that would stop a sweeping commit is registered and unverified —
    and it is also a hazard for any reviewer asked to plant probes here, since two agents mutating
    `src/` can contaminate each other's measurements. Worth one line in the milestone handoff.
  * **S10 (NIT).** Criterion 2's buckets do not sum to its own total: CUT 5 + POINTER 1 +
    TRIGGERED 0 + KEEP 94 + FIX-IN-PLACE 1 = **101** against "100 facts inventoried mechanically".
    Presumably FIX-IN-PLACE is a subset of KEEP, which would make it 100 — but the record does not
    say so, and the whole paragraph's authority is that the inventory was mechanical.
  * **S11 (NIT).** Sweep 1's measurement table is denominated in **15** definitions ("0 / 15",
    "1 / 15", "15 definitions, 1,075 lines"); the tree ships **12**, `install-agents` plans 12,
    `.claude/agents/` holds 12, and 12 held 12 at `799adfa^` too. "What landed" silently switches to
    twelve. A reader comparing the two sections cannot tell whether three definitions were dropped
    or the 15 was always wrong. One clause reconciling it.

## Criterion 3, measured a third time — my own harness observation

I am a fresh agent spawned into this checkout, and the contract asked what my context held at spawn
and whether a Bash read and a `Read` differ. Both, unprompted, before I read the grain's claim.

**At spawn my context held:** the user's global `~/.claude/CLAUDE.md`, the auto-memory index
(`MEMORY.md`), the skill listing with one-line descriptions and no bodies, and the tool schemas.
**It did not hold this project's `CLAUDE.md` and did not hold `.claude/rules/pm-execution.md`** — I
had to read both. My working directory is `/Users/cdowin/workspace/agentic-sdlc`, i.e. the checkout
itself, which is the case the earlier sessions could not test. **The spawn measurement reproduces at
n=3, and now also from a session rooted AT the checkout rather than at its parent.**

**The A/B, with the Bash read first, same as the grain's:**

| call | file | injected |
|---|---|---|
| `Read` | `/Users/cdowin/workspace/agentic-sdlc/CLAUDE.md` (repo root) | nothing |
| Bash `sed -n` | `pm/roadmap/features/ft-a-surface-…md` | nothing |
| `Read` | `pm/roadmap/milestones/ms-…-decisions.md` | `.claude/rules/pm-execution.md`, in full |

**Doctrine confirmed, and the grain's conclusion corroborated: a `paths:`-scoped rule fires on `Read`
and does not fire on a Bash `sed -n`.** My third row sharpens it — the rule's frontmatter is
`paths: ["pm/roadmap/**"]`, and the `Read` of `CLAUDE.md` at the repo root injected nothing, so the
trigger is the glob and not merely the tool. TRIGGERED = 0 stands on my evidence too, and for the
grain's own reason: I read this repository largely with `sed -n` and `grep`, and every one of those
reads defeated the tier.

**One divergence, and it cuts in the grain's favour.** The grain reports that its `Read` injected
*both* `CLAUDE.md` (169) and `pm-execution.md` (224). Mine injected only `pm-execution.md`. The
project `CLAUDE.md` never reached me at all — not at spawn, not on a Read of a `pm/roadmap/` file,
not on a Read of `CLAUDE.md` itself. So in this session the always-loaded tier reached zero lines and
the path-triggered tier reached 224, which is a worse result for the always-loaded file than the
grain measured, not a better one. n=1 on the divergence and I would not build on it.

## Probes, all run and reverted

Every plant was made by `cp`ing the target to
`/private/tmp/claude-501/-Users-cdowin-workspace/9dd9bd25-745d-418d-9045-f0cd9c95b7f5/scratchpad/`,
editing in place, measuring, and restoring from the copy. `git status --porcelain` was asserted after
each group and is clean for `src/` and `tests/` at the end; the only modified paths in the tree are
the four `pm/roadmap/` files that were already modified when I arrived. Unit tier before:
**1130 passed, 3 skipped, 435 deselected, 786 subtests, 16.69s**. After all plants and restores:
**1130 passed, 3 skipped, 435 deselected, 786 subtests, 16.83s**. `check pm`, `check doc`,
`check hooks` and `check grain-shape` each exit 0.

| planted | what happened |
|---|---|
| **D7 / prose census** | |
| dispatcher prints `'a gate.'` instead of `module.__doc__` | exclusion **9 modules/214 lines → 2/71**; claim reproduced exactly |
| +277 plain comment lines in `checks/shell.py` | **RED** — `assert (4737 / 14210) < 0.3333`; claim reproduced exactly |
| +277 essay lines inside `checks/shell.py`'s *printed* module docstring | **GREEN** — ungraded ratio 0.35384, headroom 276.7 → **369.0** (S3) |
| new module `_probe_unprinted.py`, docstring copied from `checks/shell.py` | **excluded** though nothing routes, imports or prints it (S2) |
| 7 plausible short docstrings vs the printed corpus | 1 of 7 collides (`'Static gates'`); corpus holds 766 distinct lines |
| **Criterion 1 / the verb resolver** | |
| `agentic-sdlc butterfly` | **RED** — `developer.md:112 … routes no agentic-sdlc butterfly` |
| `agentic-sdlc pm butterfly` | **RED** — named at `:113` |
| `agentic-sdlc check butterfly` | **RED** — named at `:114` |
| `agentic-sdlc close milestone` | **RED** — named at `:115` |
| `agentic-sdlc lesson forget` | **RED** — named at `:116` |
| `agentic-sdlc verify --everything` | **RED** — named at `:117` |
| `agentic-sdlc pm ready-for epic` | **RED** — named at `:118` |
| `pm story building st-a-thing` / `pm feature reviewing ft-x` | **GREEN** — project state words not graded, correct |
| `pm list --status building,reviewing` / `check pm --fix` / `pm --help` | **GREEN** — flags not graded, correct |
| `close story st-a-thing` / `pm ready-for story st-x` / `pm decide ft-…` | **GREEN** — ids not graded, correct |
| `pm set <id> owner <name>` / `verify --story` | **GREEN** — placeholders and a real mode, correct |
| `pm ledger frobnicate` + 4 siblings at ungraded positions | **GREEN — missed** (S4) |
| **D6 / `check hooks`** | |
| settings file registering nothing (`install-hooks`, no `--write-settings`) | exit **0**, `NONE of the 5 cc-hook(s) is registered`, each named |
| `--write-settings` run | `5 of 5 … registered`, `not IN FORCE`, `session's project root` |
| settings registering only via `permissions.allow` | exit **0**, `NONE … registered` — allowlist correctly not a registration |
| settings file that is not JSON | `could not be read`, `it is not JSON` |
| `settings_commands` → `path.read_text()` (text reader) | **RED** on the allowlist case — the guarded shape, confirmed |
| `settings_commands` → `_commands(data)` (whole-document walk) | **GREEN** on all cases — unguarded shape (S8) |
| **Q4 / the router corpus** | |
| `_resolve` → `return set(), []` (the silent drop) | **RED** — replay names both shapes routing through it |
| `routed_verbs()` re-inlined to call `_router_branches` | `test_cli_surface.py` **1 shaped guard → 5**, 4 newly uncovered (S5) |

## What I did NOT verify

  * **Whether the `cc-*` guard hooks are actually in force when the project root IS the checkout** —
    D6 names this as owed and it stays owed. My session is exactly that case, but the only decisive
    probe is a `git commit` with no pathspec: `--dry-run` is on the hook's exempt list, and
    `cc-write-confine.sh` exits 0 for a target in no repository, so my scratchpad writes prove
    nothing. I declined the `git commit` form because my instructions forbid mutating git commands
    and a concurrent agent held this index (S9). This is the single largest gap in the review, and it
    is the same gap the feature reports.
  * `Edit`, `Grep` and `Glob` against the `paths:` rule. I tested only `Read` and Bash `sed -n`.
    Every harness observation here is n=1.
  * The second repeated correction sweep 3 names — `make test` not including `matrix`, "four agents
    reported green against a gate the release belt actually runs". Nothing in this changeset
    addresses it and I did not chase whether it should have.
  * Sweep 4 / the README. Scoped out in writing, which is legitimate; I did not grade the 401 lines
    or the claim that the `## Verbs` table restates `--help` for 25 verbs.
  * Whether anything gates `--help` output length. I found no such gate while investigating S3 but
    did not search exhaustively, so "nothing caps the exclusion" is a strong inference, not a proof.
  * Byte-currency of the twelve installed `.claude/agents/` copies against their sources. That is
    `test_install.py`'s existing job and the unit tier is green; I ran the resolver cases narrowly
    with `-k` and did not re-read the diff of all 24 files line by line.
  * The full `shell` tier, `make test`, `make milestone` and `make matrix` — out of bounds by the
    contract. My shell-tier runs were narrow `-k` selections in `test_check_hooks.py` only.
  * The 100-fact inventory behind criterion 2. I verified the line counts, the rule ordinals 1–11
    (all present, in order, at lines 16–63), the FIX-IN-PLACE claim (`src/agentic_sdlc/repo/cli.py`
    does not exist, `src/agentic_sdlc/repo/pm/cli.py` does, and both sites in `CLAUDE.md` now name
    the path in full) and the bucket arithmetic — not the classification of the individual facts.
  * The other eight features of this milestone, and the `ft-the-vocabulary-is-constants-not-literals`
    review. Both out of scope; the cross-cutting pass runs after this.

## Landed after the record was written

**S1 (MAJOR), the one that blocked the close.** The claim was wrong, not the work. Criterion 4 is
re-attributed to `test_every_verb_an_agent_definition_names_resolves_against_the_cli`, which IS a
repeated correction turned into a gate that fails — the correction being six briefs that hand-pasted
the role's verbs, now two assertions, red on 7 of 7 probes. The grain says plainly that the
REGISTERED line is a named line under rule 11 and NOT a gate, gives D6's reason it must stay a count,
and keeps its finding, which is the sharp one.

**S4 (MINOR).** Fixed the way the reviewer described it, because the reviewer was describing this
feature's own move. `pm/cli.py` spelled `(record, show, report)` as a literal in the refusal beside
the if/elif chain that implements it; it is now `LEDGER_RECORD|SHOW|REPORT` behind
`ledger_commands()`, the refusal renders from it, the dispatch is a table, and
`('pm', 'ledger')` joined `_verb_rosters()`. Probed: `pm ledger frobnicate` planted in
`architect.md` now reports `architect.md:50 cites … — this package routes no
'agentic-sdlc pm ledger frobnicate'`. `pm ledger frobnicate` at the CLI refuses naming the roster.

**S8 (WARNING).** The other wrong reader is now guarded.
`test_a_command_node_outside_the_hooks_key_is_not_a_registration` plants a `statusLine` node — a
real `{command: ...}` node that lives outside `hooks` in a real settings file — and goes red under
`_commands(data)` in place of `_commands(data['hooks'])`, which was the refactor that passed every
other case. Both wrong readers now have a case each, and each docstring names which.

**S6, S7, S10, S11.** Every number the reviewer corrected is corrected in the grain, each marked as
the review's finding rather than quietly overwritten: the line-count outcome is 0 for the pass and
+2 for the feature; all twelve definitions grew and two are the only ones with DELETIONS; the
four-question buckets sum to 100 with FIX-IN-PLACE inside KEEP; and sweep 1's `n / 15` reads
`n / 12`, with nothing dropped.

**Carried, open: S2, S3, S5, S9.** S2 and S3 are the shape of D7's exclusion rather than its
existence — the reviewer's own verdict is that it is derived and not a rule turned off. S5 is a
finding against `test_guard_corpus.py`'s `_readers()`, not against this changeset. S9 is an
observation about concurrent agents in one worktree and belongs in the handoff, where it is going.
