# Milestone review — `0.6.0 the rule reaches the work` — RELEASE-WITH-FIXES

Cross-cutting pass over `main..HEAD` (33 commits, 168 files, +10976/−3233, 21 grains), branch
`milestone/0.6.0-the-rule-reaches-the-work`, run 2026-09-08. SDLC close-protocol step 1, and it runs
BEFORE the gate (step 3).

**Nothing below was concluded from reading a diff.** The primary instruments are two scratch trees
under the agent scratchpad: a v0.5.0 tree from `git archive v0.5.0`, and a full HEAD copy used for
defect planting. The v0.5.0 tree doubles as a scratch CONSUMER — git-initialised, committed, given a
`DEVKIT_VERSION := 0.5.0` pin — and was driven with HEAD's CLI. Both trees run stdlib-only through
`PYTHONPATH=<tree>/src python3 -m agentic_sdlc.cli`, which is how a v0.5.0 CLI can be executed at all
and how the import path is held to the tree under test. **Never `uvx --from <path>`.**

The nine per-feature review records were NOT re-run; their findings are dispositioned, and
`pm ready-for tag` reads READY over all nine (9 records, 34 findings, 7 open, none at or above
MAJOR). Three of their conclusions were spot-checked and held.

**The tree moved under this review.** Four files were dirty at the start (a concurrent reviewer's);
by the end `src/agentic_sdlc/repo/pm/cli.py`, `tests/test_check_hooks.py`, `tests/test_install.py`
and the milestone's decisions file had also changed, plus a tenth review record arrived untracked.
None of it is mine — every probe ran in a scratch copy, and `checks/doc.py`, the file this review's
BLOCKER concerns, is unmodified in the checkout.

```
verdict: RELEASE-WITH-FIXES
| id | severity | disposition |
| B1 | BLOCKER | landed in-place |
| M1 | MAJOR | landed in-place |
| M2 | MAJOR | landed in-place |
| m3 | MINOR | open |
| m4 | MINOR | open |
| m5 | MINOR | open |
| m6 | MINOR | open |
| m7 | MINOR | open |
| m8 | MINOR | open |
| m9 | MINOR | open |
| m10 | MINOR | open |
| n11 | NIT | open |
| n12 | NIT | open |
| n13 | NIT | open |
| n14 | NIT | open |
```

**This record is not bound yet, and until it is, the gate cannot see it.** `ready_for._pointers`
reads the features' `reviewed:` fields plus *the milestone's own*, never a `review_dir` sweep, and
`ms-the-rule-reaches-the-work.md` carries no `reviewed:` field — so `pm ready-for tag` currently
answers **READY, none blocking** while B1 is open. Binding it is a `pm/roadmap/` write, which this
review is not permitted to make. Verified in a scratch copy: with
`reviewed: "docs/reviews/2026-09-08-0.6.0-milestone-review.md"` set on the milestone, the same
command answers `BLOCKED  B1, M1, M2 open …` / `NOT READY … 11 record(s), 60 finding(s)`, exit 1. So
the fenced block above parses; it just needs a pointer. **Set `reviewed:` first, or the tag gate
passes green over this review.**

**Land B1, M1 and M2 before `agentic-sdlc release 0.6.0` runs.** Everything at MINOR and below is
recorded and carried. The release is separately gated by two procedural facts, neither a defect:
`ft-a-surface-reaches-its-reader-or-it-is-decoration` is `reviewing`, and
`bg-a-dispatch-nobody-records-leaves-the-spend-surface-empty` is `open` — D1 forbids the parent
closing over it, so it must reach `closed` or leave via `pm remove`.

## The semver call — MINOR (0.6.0) is the right number

Hard rule 7: *patch = same interface; minor = a new verb, flag, config key or output shape; major =
a consumer Makefile or hook must change to survive.*

**Measured against `v0.5.0`, not argued from the diff.** Three changelog entries say BREAKING. Each
was enumerated at both trees and, where behavioural, RUN at both trees.

| what could break a consumer | how it was measured | result |
|---|---|---|
| a verb withdrawn | 15 top-level + 19 `pm` subverbs routed at both trees | **none**; HEAD is a strict superset (adds `dispatch`, `changelog`, which exit 2 at v0.5.0) |
| a flag withdrawn | `--help` flag sets diffed per verb, 15 verbs | **none**; adds `--role`, `--tokens-total`, `--version` |
| a `Makefile.devkit` target withdrawn | target lists diffed at both trees | **none**; 7 targets identical |
| a conveyor step withdrawn | `Check('…')` registry + `DEFAULT_RELEASE_STEPS` both trees | **none**; 23 steps identical, same order |
| a gate withdrawn | `KNOWN_GATES` both trees | **none**; 7 gates, byte-identical on/off defaults |
| an installable withdrawn | `find` over `installables/` both trees | **one** — `changelog-writer.md`; declared in `install.RETIREMENTS` |
| a grepped output shape withdrawn | prefix sets of `pm status`/`validate`/`list`, same tree, both CLIs | **none** added, none removed |
| exit-code parity | `verify`/`check`/`lesson` `--help` both trees | identical (0/2/0) |

**Claim 1 — `fix_milestone:`/`caught_in:` retired. Not breaking; D2's ruling is exactly what the code
does.** On the scratch consumer, 24 bug documents carry the retired fields. With `D11` declared,
`check pm` exits **1**, names each field with the reason and the remedy, and still grades everything
else (`CONTAINMENT 173 bound child/ren graded`). Exit 1, not 2, on a real tree — verified, not taken
on the ruling's word.

**Claim 2 — `pm new milestone` version positional → `--version`. This claim is FALSE, and it is the
one measured result that contradicts a shipped sentence.** At v0.5.0 the signature is
`new milestone <slug> <name...>`; there was never a version positional. Run at both trees:

```
v0.5.0:  pm new milestone 0.9.9 "probe milestone"  ->  created ms-0.9.9.md   exit 0
HEAD:    pm new milestone 0.9.9 "probe milestone"  ->  created ms-0.9.9.md   exit 0
```

Identical output, identical id. `--version` is a pure addition that decouples slug from version. The
changelog's *"so a version no longer becomes a milestone's id"* is false — it still does, if you pass
it as the slug. The grain itself says so (*"still parses and still mints `ms-0.7.0`"*), so the
changelog contradicts its own grain. See M2.

**Claim 3 — `CHANGELOG.md` retired. Not a withdrawal of consumer surface.** The step
`changelog-unreleased-nonempty` and the `[<operation>] changelog` config key both SURVIVE at HEAD;
the step deliberately keeps its name, and says why in its own docstring (*"a step id is contract
(rule 6)"*). What changed is what it reads. A v0.5.0 consumer has zero `changelog:` fields — HEAD's
verb reports `0 entry/ies of 24 grain(s) … 24 unanswered` — so their `release` now refuses until they
backfill. That is a DATA migration, not a Makefile or hook change.

**The one thing that does break, and the precedent for it.** A consumer whose `devkit.toml` names the
retired `D3` goes from green to a hard stop: `check pm` and `pm validate` exit **2**, naming D11 as
the replacement and saying "Remove it from the list." This is exactly the shape 0.3.0 hit with `D8`,
and that review still called MINOR the right number. No Makefile and no hook must change. The adopt
belt surfaces it twice (`checks-pass` and `pm-validates` both unverifiable, quoting the full
instruction), so it is loud rather than silent.

**Conclusion: nothing in rule 7's major clause fires. `0.6.0` is correct.** Two output shapes did
change — `check pm`'s warning family, and three shapes the warning feature names in its own changelog
line as "so minor" — and rule 6 puts an output-shape change at minor, which this bump already is.

## Does the milestone meet its own ship criteria?

| # | criterion | verdict |
|---|---|---|
| 1 | contracts reach a dispatched agent from a generated surface | **MET** — `dispatch` renders a 37-line preamble; ladder, gate roster and vocabulary all derived from `[verify]`/`[checks]`/`[pm.states.*]`. This review's own briefing is that surface. |
| 2 | every source-shaped guard declares a violation corpus; one without is named | **MET** — `tests/test_guard_corpus.py` derives the roster and gates BOTH directions. Probed: deleting one `UNCOVERED` entry turns it red. |
| 3 | no shipped file states what a verb does in prose the verb does not generate | **SCOPED, and the scoping is honest — but its only mechanised slice is ungated. See B1.** |
| 4 | `check pm`'s warnings are actionable where they fire | **MET** — same tree, both CLIs: **48 warnings → 2**, while HEAD simultaneously surfaces **52** drift violations v0.5.0 was blind to. Fewer warnings, stricter rules. |
| 5 | a bug binds to its parent alone; `release` refuses on the field the scaffold writes | **MET** — `ready-for milestone` refuses on `milestone:`, naming the open bug. |

Criterion 3 is a universal negative and could not be met literally. The grain says which half it does
not cover, out loud: *"A sentence paraphrasing what a verb DOES … not mechanisable here."* That is
legitimate scoping. The defect is not the scope — it is that the half which DID land has no gate on
its own wiring.

## Is the tree honest about what did NOT get done?

**Substantially yes, in the grains; not in the milestone's own narrative documents.** Four grains
carry a "What landed, and what did not" section. Across those sections, the two scoped bug fixes and
the three BREAKING lines, 34 discrete claims were checked: **33 true, 1 false** (M2). Every "did not
land" claim is genuinely absent from the tree — the read-verb feature's collect-and-render primitive,
the vocabulary sweep's unified `EXIT_*` home and the `cli.py`/`model.py` split, all verified absent
rather than quietly shipped.

**Sweep 4 was dropped whole and said so three times.** A repo-wide search found no file anywhere
claiming it landed. `bg-a-dispatch-nobody-records-…`'s "What this does NOT fix, and it is the larger
half" is the most honest writing in the tree, and `check pm` U4 confirms its admission live.

What is not honest is the milestone file and the handoff — see M1. Those documents were written at
planning time, were true then, and were never reconciled at close.

## Findings

### B1 — BLOCKER — `check doc`'s invocation rule is wired by one line no test exercises; unwire it and the whole suite stays green — `open`

Ship criterion 3's only mechanised slice is `check_invocations`, the rule written to catch the exact
0.5.0 defect where an auto-loaded file instructed `pm story reviewing`, which exits 2. Six new cases
prove it, in `tests/test_check_doc.py::AnInvocationIsAClaimAboutTheTree`. All six route through one
helper that calls the classifier FUNCTION directly. Nothing in the range calls `doc.run()`, the
gate's entry point. The single line joining rule to gate is
`src/agentic_sdlc/repo/checks/doc.py:213`.

Planted in a scratch HEAD copy — that one line deleted, rule and tests otherwise untouched:

| | 6 invocation cases | unit tier | `check doc` on a doc holding the 0.5.0 sentence |
|---|---|---|---|
| unwired | **6 passed** | **1130 passed, 3 skipped, 786 subtests** | `PASS — 1 doc(s) … 0 unresolved claims`, **exit 0** |
| wired (HEAD) | 6 passed | 1130 passed | names the violation, **exit 1** |

This is hard rule 4's first cardinal sin — a gate that misses drift and prints PASS — latent inside
the milestone whose northstar is *a gate that cannot fail is not a gate*, and inside the check built
to catch a shipped-sentence defect. **The gate works at HEAD today**; what is absent is any gate on
its wiring, which is precisely the class this milestone hunted and fixed three times. The milestone's
own Risks section sets the standard: *"If 0.6.0 ships a rule nobody can reach, it has disproved
itself."*

Fix: one case that runs `doc.run()` over a scratch tree and asserts exit 1 — the shape
`tests/test_check_hooks.py` already uses. Small, and worth blocking on because the fix is small.

### M1 — MAJOR — the milestone's planning-time narrative was never reconciled at close; three instances — `open`

One omission, three places. Each was true when written and is false now, which is exactly the
staleness the milestone's own argument predicts.

1. **`ms-the-rule-reaches-the-work.md:121,126`** — *"**Six** are bound to this milestone"* and
   *"**four** bugs now declare no milestone at all"*. `pm ready-for milestone` reports **11 bugs
   nested** and **6 attached to no milestone**. The paragraph declines to copy the roster *"because a
   roster copied into prose is the second scoreboard 0.3.0 retired `ROADMAP.md` for"* — and then
   hand-writes the counts. The fix its own principle demands is to delete the numbers, not update
   them.
2. **`ms-the-rule-reaches-the-work.md:165` (Risks)** — *"`pm new milestone <ver>` **still mints** the
   VERSION as the id … That is the unlanded half of #8"*. `bg-the-milestone-scaffold-still-mints-the-version`
   is `status: closed`, position 3 in `order:`. The risk is stale and unstruck.
3. **`ms-the-rule-reaches-the-work-handoff.md:42`** — *"Filed as `bg-the-prose-ceiling-has-no-headroom`,
   **unbound in the pool**."* It is `milestone: ms-the-rule-reaches-the-work`, position 21 in
   `order:`, `status: closed`. This is the cold-start document telling a fresh session that D7 — the
   most consequential ruling in the milestone — is unfixed and ungated.

The handoff's stated bar is *"a fresh session with no context picks up from the tree alone."* On
these three points it would pick up wrong.

### M2 — MAJOR — a consumer-facing changelog line is labelled BREAKING and states something measurably false — `open`

`bg-the-milestone-scaffold-still-mints-the-version`'s `changelog:` reads *"BREAKING: … the first
positional is the slug the id is minted from, **so a version no longer becomes a milestone's id**."*
Run at HEAD, `pm new milestone 0.9.9 "probe milestone"` creates `ms-0.9.9.md` and exits 0 — a version
still becomes a milestone's id. The grain's own body says so plainly (*"still parses and still mints
`ms-0.7.0`"*), so the rendered changelog contradicts the grain it is rendered from.

The error direction is safe — it over-warns rather than under-warns — but `agentic-sdlc changelog` is
the consumer-facing release note, and this is the surface a consumer reads to decide whether they
must migrate. Fix: drop the BREAKING label and the false clause; the accurate line is that
`--version` is added and stamps `version:`.

### m3 — MINOR — a contract case uses a weak substring test where its four siblings use the strong one — `open`

`tests/test_contracts.py::EveryReadVerbsHelpNamesItsColumns::test_pm_list_help_names_every_kinds_columns`
tests each column with `c in text` against the whole 14,270-character `pm` USAGE, while its four
siblings in the same class use the joined-in-order `_named()` helper. Measured: strip all three
`columns IN ORDER` lines from `pm list`'s help and it still reports 5/5 for story, feature and bug —
only `milestone` fails, incidentally, because `branch` appears nowhere else. It also cannot see column
ORDER, which is the declared property. Not the fourth gate — `tests/test_cli_surface.py` covers the
subject — but it should use `self._named(...)`.

### m4 — MINOR — a shipped comment says the retirement table is empty; it is not — `open`

`src/agentic_sdlc/repo/install.py:607`: *"The table is EMPTY because nothing this package ships was
withdrawn between 0.2.0 and 0.3.0"*. Nine lines below, `RETIREMENTS` carries the 0.6.0
`changelog-writer.md` row this milestone added. A shipped sentence contradicting shipped behaviour,
in the milestone named for that defect.

### m5 — MINOR — the guard-corpus grain and its gate both say 15 where the tree says 16 — `open`

`ft-a-guard-declares-its-violation-corpus.md:88` says *"15 of 30 AST-shaped guards declare a corpus;
the other 15 are named in `UNCOVERED`"*, and `tests/test_guard_corpus.py:89` says *"The fifteen
below"*. Measured with the module's own reader: **34 guards, 18 covered, 16 uncovered**, and
`len(UNCOVERED) == 16`. Line 85's *"replaying 155 corpus cases"* against 182 subtests today. The
`MIN_*` comment at :76 is honestly hedged with "at the time of writing"; these are not.

### m6 — MINOR — the handoff points at a section that does not exist — `open`

`handoff.md:53-57` says two scoped criteria are *"**both** written into their grain files under
'What landed, and what did not' — **look there before assuming a criterion was met**."* Only one is.
`ft-prose-that-restates-a-verb-is-rendered-or-gone.md` has no such section; its scoping lives under
`## What is mechanisable, and what is not`. The content is present and honest — a reader following
the handoff's own instruction finds nothing.

### m7 — MINOR — "the line-count outcome is −1" is not what the tree produces — `open`

`ft-a-surface-reaches-its-reader-or-it-is-decoration.md:159`, and the same claim in the commit
message. `git show --numstat 7b6a13c -- CLAUDE.md` is 6 added / 6 removed, **net 0**; across the
milestone `CLAUDE.md` went 169 → 171, **+2**. The surrounding argument (the file was already under
its target) is unaffected and if anything understated.

### m8 — MINOR — the handoff's `checks/doc.py` count is wrong — `open`

`handoff.md:50` says *"`checks/doc.py` still holds **12 other** module-level `REPO_ROOT` uses"*.
Actual: 8 mentions, of which **6** are uses.

### m9 — MINOR — the D3 retirement message instructs an edit that leaves containment ungated — `open`

The message names D11 (*"became D11 in 0.6.0"*) and then instructs *"Remove it from the list."* A
consumer with an EXPLICIT `[pm] checks` list who follows it exactly gets `check pm` **exit 0** with 24
bug documents still carrying retired fields and containment graded by nothing — measured on the
scratch consumer. Adding D11 turns it to exit 1 with 52 real findings. Mitigations: the stock
`DEFAULT_CHECKS` and the shipped seed both include D11/D12, so only consumers with an explicit list
are exposed, and the replacement is named in the sentence. Fix is one clause: "Replace it with D11."

### m10 — MINOR — `dispatch --role` is a label, and role content now exists one directory away — `open`

`--role` changes exactly one line, the header, and an unknown role (`--role not-a-real-role`) is
accepted and stamped into the contract. The per-feature review already named the label-only behaviour
and I do not re-litigate it. What is new at this level is that `aaede29` landed AFTER that review and
gave 12 shipped agent definitions a `role-verbs` block naming the verbs each role reaches for — and
no module reads them. Two 0.6.0 surfaces, one writer and one reader, that never meet: the shape this
milestone is named for. Nothing false is claimed, which is why this is MINOR.

### n11 — NIT — a rule-4 floor that is unfailable by construction — `open`

`tests/test_install.py::test_every_declared_retirement_names_a_routed_verb_and_a_readable_version`
asserts `checked >= len(FIXTURE)` where `checked` increments once per row of
`install.RETIREMENTS + FIXTURE` — true by construction, and it passes at zero. Same family as the
`assertIn(x, X)` tautology already fixed in this range. The subject is really covered by its sibling
`test_the_shipped_table_names_the_changelog_writer_at_0_6_0`.

### n12 — NIT — `changelog.Entry.declined` case-insensitivity is untested — `open`

`NEEDS_NONE` is compared with `.lower()` and the comment gives the reason ("typed by hand"), but no
case plants `None`/`NONE`; every test uses lowercase `none`.

### n13 — NIT — internal denominator drift inside the surface feature — `open`

Sweep 1 tabulates "0 / **15** definitions"; the What-landed section says "0/**12**" and "**Twelve**
`role-verbs` blocks". Actual is 12. Same file, `:209`: the README's `## Verbs` table "restates
`--help` for **25** verbs" — the table has **27** backticked rows.

### n14 — NIT — the `name-both-commands` block in five agent installables has no content assertion — `open`

Only marker presence, byte-identity across the five, and absence in three others are asserted. An
identically-gutted block would survive every gate except source/installed byte-currency. **Probe not
completed** — it needs ten coordinated edits — so this is reported as an unverified suspicion, not a
measured finding.

## What holds

Worth stating, because most of what I probed was sound.

- **Self-hosting: 29 of 29 installer destinations byte-current**, across all six installers, zero
  drifted, zero absent, zero even header-only differences. `make check` exit 0 in 3.2s (5 gates).
  `make test` exit 0 — **1565 passed, 3 skipped, 868 subtests in 44.0s**, inside the 150s budget.
- **Six of seven gates exit 0.** `check repo-hygiene` exits 1 on the concurrent agent's dirty files
  plus three merged-but-undeleted branches; it is off `[checks] all` and off `DEFAULT_RELEASE_STEPS`
  by declaration, so it does not gate the tag. The stale branches are close-time housekeeping.
- **D7 is a correction, not a rule turned off.** Five probes, below. The ceiling is unchanged at 1/3,
  all nine excluded modules are genuine `--help` surfaces, the census still reddens on real
  over-commenting, both rule-4 guards on the exclusion fire, and the exclusion is derived rather than
  a roster. I went looking for a relaxation and did not find one.
- **The `semver-gate.yml` success path now fires.** I extracted its shell and drove all three paths
  locally: refuse-not-done, refuse-zero-scan, and the success path that matches on `version:` where
  the old id-only test could never match `0.6.0` against `ms-<slug>`.
- **The adoption path is completable, and every step is actionable.** exit 2 (config) → migrate
  `devkit.toml` → exit 1 with 52 findings naming each field and its remedy → strip the retired fields
  → 8 genuine containment findings, which is D1's real backfill. `adopt 0.6.0` names all five false
  checks with the `--diff` command per file.
- **The withdrawn installable is handled.** Post-bump simulation (scratch package stamped 0.6.0)
  produces: *"no longer written: `.claude/agents/changelog-writer.md` — withdrawn between v0.5.0 and
  v0.6.0. This verb writes a set and never deletes, so the file is still in your tree."*
- **`check budget` names `integration` UNMEASURED and UNCOUNTED** rather than passing silently —
  rule 11 satisfied.

## Probes, all run and reverted

Every plant was made in a scratch copy under the agent scratchpad, never in the checkout.

| # | probe | where planted | expected | observed | reverted |
|---|---|---|---|---|---|
| 1 | 15 verbs / 19 subverbs routed at both trees | — (enumeration) | no withdrawal | 0 withdrawn | n/a |
| 2 | `--help` flag sets diffed, 15 verbs | — | no withdrawal | 0 withdrawn; +3 added | n/a |
| 3 | `pm new milestone 0.9.9 "probe"` at v0.5.0 | scratch consumer | mints `ms-0.9.9` | `ms-0.9.9`, exit 0 | tree reset |
| 4 | same invocation, HEAD CLI | scratch consumer | breaks, per changelog | **identical, exit 0** → M2 | tree reset |
| 5 | retired fields, D11 undeclared | scratch consumer `devkit.toml` | drift named | **exit 0, silent** → m9 | tree reset |
| 6 | retired fields, D11 declared | scratch consumer `devkit.toml` | exit 1 drift (D2) | exit 1, 173 children graded | tree reset |
| 7 | `adopt 0.6.0` | scratch consumer | names the migration | 5 checks false, D3 named twice | tree reset |
| 8 | full migration (config + strip fields) | scratch consumer | completable | 52 → 8 findings, exit 1 | tree reset |
| 9 | post-bump retirement report | scratch pkg stamped 0.6.0 | names the orphan file | named, with remedy | scratch |
| 10 | 277 plain comment lines | scratch `pm/model.py` | census RED | **0.33336, RED** | reverted |
| 11 | exclusion derives nothing | scratch, monkeypatched | guard fires | RED, guard fired | scratch |
| 12 | exclusion swallows everything | scratch, monkeypatched | guard fires | RED, guard fired | scratch |
| 13 | withdraw `print(module.__doc__)` | scratch `cli.py:145` | exclusion shrinks | **9→2 modules, 214→71 lines** | reverted |
| 14 | delete one `UNCOVERED` entry | scratch `test_guard_corpus.py` | gate RED | RED at :390 | reverted |
| 15 | orphan key in `ledger.ROW_KEYS` | scratch `ledger.py` | contract gate RED | **RED** — `SUBFAILED(key=…)` | reverted |
| 16 | same, WITHOUT `PYTHONPATH` control | scratch `ledger.py` | — | green — **my probe was invalid**, see note | reverted |
| 17 | `semver-gate` shell, 3 paths | extracted to scratch | refuse/refuse/pass | all three correct | n/a |
| 18 | **unwire `check_invocations` from `run()`** | scratch `checks/doc.py:213` | suite RED | **GREEN — 1130 passed** → B1 | reverted |

Probe 16 is recorded deliberately. Run from a scratch tree without `PYTHONPATH` pointing at that
tree, `uv run --project <repo> pytest` imports the REAL package, so the plant is never loaded and the
probe silently tests the wrong tree. I caught it by printing `module.__file__` and re-ran; the gate
was sound. It is the same defect class as B1 — a check that appears to be exercising something and is
not — and any future reviewer planting defects here should print the import path first.

A parallel sweep enumerated 143 candidate cases (103 added defs + 40 modified bodies) across 21
changed test modules and planted 10 defects; 8 went red, 1 red-but-elsewhere, 1 green (B1). Its
probes 1–9 covered the vocabulary sweep's literal guards, the rule-2 spawn guard, the dict-key reader,
`pm list` columns and the semver gate's `version:` read.

## What I did NOT verify

- **`make milestone` and `make matrix` were not run**, by instruction — the release belt runs the one
  authoritative full gate after this judgement lands. Nothing here speaks to gates that live only in
  the widest rung.
- **The nine per-feature review findings were not re-litigated.** I read their verdicts and
  dispositions only.
- **No CI workflow was executed in GitHub Actions.** I drove `semver-gate.yml`'s shell locally by
  extraction; `auto-tag.yml` and `verify.yml` were read, not run. Whether the runner's environment
  matches my extraction is unverified.
- **The `integration` tier has never run in this tree** — `check budget` reports it UNMEASURED and
  UNCOUNTED. Its 130s / 430-case ceilings are untested, so I cannot say the suite's ~114 new cases
  left it inside budget.
- **n14 is an unfinished probe**, reported as a suspicion.
- **The vocabulary sweep's `603 → 60` literal counts were not re-measured** (disputed as V-series
  findings in that feature's own review, dispositioned there), nor was its AST value-preservation
  proof.
- **The 155 → 182 corpus-case delta's cause** was not traced, and I did not check whether each of the
  16 uncovered guards still grades.
- **Hook execution was not observed.** The session's project root is the checkout's parent, so no
  hook fires here; I accept that framing, which `check hooks`'s REGISTERED line now states rather than
  asserts around.
- **The tree changed under me.** `src/agentic_sdlc/repo/pm/cli.py`, `tests/test_check_hooks.py` and
  `tests/test_install.py` were modified by a concurrent reviewer after my gate runs. My `make test`
  and `make unit` numbers predate those edits, and B1 was reproduced against `HEAD`, not against the
  working tree.

## Landed after the record was written

**B1 (BLOCKER).** `TheRuleIsWiredIntoTheGate` in `tests/test_check_doc.py` runs `doc.run()` over a
scratch tree carrying the exact sentence the rule exists to catch, and asserts exit 1 and the
finding — plus the other direction, a declared state, on the same surface, so a gate that reported
every invocation would not pass. `doc.REPO_ROOT` is rebound for the duration rather than converted,
because converting it is the deferred vocabulary sweep's job. **Probed the reviewer's way**: deleting
`checks/doc.py:213` now fails exactly one case, and it is this one.

**And a second gap of the same shape, found while writing it.** `_STATUS_FORM` was anchored at
`^pm\s+`, so `agentic-sdlc pm story reviewing` — the fuller form the README and all twelve agent
definitions use — was read as prose and never graded. The rule was correct, wired, and unable to see
half the surface it covers. The prefix is optional now; `check doc` still passes on this tree.

**M1 (MAJOR).** All three sites reconciled. The bug tallies are gone from the milestone's prose
entirely rather than corrected — copying a TALLY into prose is the same defect as copying a roster,
one size down, and it went unnoticed in the paragraph that forbids it; the line now points at the
command. The Risks entry says the scaffold defect CLOSED here and is left standing rather than
deleted, because a risk that was real and got fixed is worth being able to find again. The handoff
says `bg-the-prose-ceiling-has-no-headroom` is bound and closed at D7, and two more of its traps
were brought current — the guard hook that was never running, and the dispatch ledger's reach half.

**M2 (MAJOR).** The `BREAKING:` prefix is off `bg-the-milestone-scaffold-still-mints-the-version`'s
changelog. The grain's own body already said `pm new milestone 0.7.0 "Name"` still parses and still
mints `ms-0.7.0`; the line now says what actually changed — where the version is RECORDED — and
names the review that corrected it.

**Carried, open: m3-m10 and n11-n14.** None blocks, and the reviewer's own note on the release gate
is acted on: the milestone now carries `reviewed:`, so `pm ready-for tag` can see this record rather
than answering READY over a blocker it never swept for.
