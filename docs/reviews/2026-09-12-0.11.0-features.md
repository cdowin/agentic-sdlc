# 0.11.0 — feature review (the three features, one pass)

**Verdict: SHIP-WITH-FIXES.** Range: 5d25397, fcda478, 48dd63e, 9eb7568. I ran the probes in a
scratch `git archive 929a9b5` copy with `uv run --project <this tree>`. I did not run `make unit`
or `make test`. The cross-cutting reviewer-scope contradiction is in the milestone record (M1).

## Blockers

- F1 MAJOR src/agentic_sdlc/repo/preflight.py:94-101: `resume` prints `allowed` when one settings
  file allows `SendMessage` and the other cannot be read. A deny in the unread file would win, so
  the fact was never read. The criterion says "`unknown` where a fact is not readable". Probe B:
  `.claude/settings.json` allows it and `.claude/settings.local.json` holds `{{{`, and the output
  is `subagent-resume	allowed	… settings.local.json could not be read`. Fix: if `unread` is
  non-empty and no deny was found, return `UNKNOWN`. Amend the `unreadable` subTest at
  tests/test_preflight.py:73 with an allow in the other file.
- F6 MAJOR pm/roadmap/features/ft-the-kit-ships-its-planning-skills.md:47: `## Ship criterion` is
  still the placeholder `<!-- Written when 0.11.0 is decomposed. -->`, and `check pm` WARNs it. So
  "done" means nothing on the record, and I judged this feature against its body. Fix: write the
  criterion before `close feature`. Proposed: *after `pm install-skills`, `writing-plans` and
  `executing-plans` are installed with a `## Project config` block a plain re-run keeps; the
  architect, po, developer, verification-builder, reviewer and milestone-reviewer briefs each
  carry the rule; `tests/test_install.py` proves them byte-current.*

## ft-the-session-says-what-it-can-do-before-the-first-dispatch (#40)

| criterion | status | shown by |
|---|---|---|
| four rows at SessionStart, in order, exit 0 | met | scratch `preflight`: 4 rows, exit 0; hook `--self-test` OK |
| subagent-resume read from `.claude/settings*.json`, not argv | met | probe A: local deny + project allow gives `denied` |
| `unknown` where a fact is not readable | **not met** | F1, probe B |
| hooks through `check hooks`' own reader, missing named | met | `hooks.cc_registration`; probe C names `cc-zz-new.sh` |
| attribution 0/1/N plus what the fallback does | met | tests/test_preflight.py:104; scratch printed `0` |
| subagent-channel row | met | preflight.py:165 |
| usage error exits 2 | met | probe D: `unexpected argument(s) x`, and preflight.py:178 returns 2 |
| launch flag is `unknown`, never a guess | met | preflight.py:102; test_preflight.py:58-61 |

Rule 2: the verb reads two JSON files and the tree, and spawns nothing. test_preflight.py is held
to the unit tier by nodeid. The hook runs the verb through `make sdlc`, the same pattern as the
couriers.

- F2 MINOR preflight.py:99-101: the `allowed` meaning mentions only the launch flag as a hedge.
  User-level and managed settings are also unread (rule 8), and a deny there wins too. Add that
  clause.
- F3 NIT preflight.py:115-119: when the file that holds a registration cannot be read, `hooks` says
  `not wired` (the tail names the unread file). `unknown` would match F1.
- F4 NIT preflight.py:149,156: the verb reaches `pm_cli._tree_snapshot`, a private helper in
  another module. Make it public beside `ledger.STORIES_IN_PROGRESS`.
- F5 NIT install.py:314: SessionStart is wired sync with no `timeout`. On a consumer's first
  session after a bump, `make sdlc` downloads the pin through `uvx` before the session starts.

## ft-the-kit-ships-its-planning-skills (#42)

| criterion (none written, F6: judged against the body) | status | shown by |
|---|---|---|
| `writing-plans` and `executing-plans` installed | met | skills.py GUIDANCE_PLAN; test_install.py:1215 loops the plan |
| project-config block kept by a plain run, and the run is idempotent | met | probe: an edited block survives a stale-body rewrite; the second run writes nothing |
| rule carried in the briefs (#15), not only the skill | met | 5d25397: architect, po, developer:10, verification-builder, reviewer, milestone-reviewer |
| no consumer stack material (rule 8) | met | grep of the three new guidance files, preflight and the hook: 0 hits |

- F7 MINOR src/agentic_sdlc/repo/installables/po.md:55-67: checklist step 1 ("scaffold two or
  more stories") and step 4 (a 30-80-line body with Scope and line ranges) still apply
  unconditionally. They sit below the new line 38 ("plan only when needed") without reconciling
  with it. A po working its checklist top-down still decomposes. Fix: gate steps 1 and 4 on "not
  already outlined".

## ft-use-the-sdlc-get-to-work

| criterion | status | shown by |
|---|---|---|
| "use the sdlc, get to work" matches the skill description | met | run-the-sdlc.md:3; architect.md checklist 1; pm-execution.md:27 |
| body names each of 9 steps with its command | met, with a gap | 7 of 9 name a command; steps 5-6 are practices (F9) |
| installed architect brief dispatches no po, scout or spec review for outlined work | met | installables/architect.md:40-43, 66-68 |
| test_install.py proves the skill and brief byte-current | met | test_install.py:1215 (skills); install-agents currency (not run by me) |

The shipped words match the owner's intent: no re-planning pass, one developer per lane, a short
brief, no wide gate, and a builder that stops at a committed branch. The 9eb7568 dispatch
preamble agrees with step 3.

- F9 NIT run-the-sdlc.md:35-38: steps 5 and 6 name no command. Reword the criterion or accept.

```text
verdict: SHIP-WITH-FIXES
| id | severity | disposition |
| F1 | MAJOR | open |
| F2 | MINOR | open |
| F3 | NIT | open |
| F4 | NIT | open |
| F5 | NIT | open |
| F6 | MAJOR | open |
| F7 | MINOR | open |
| F9 | NIT | open |
```
