# 0.11.0 — milestone review (ms-a-session-starts-knowing-what-it-can-do)

**Verdict: RELEASE-WITH-FIXES.** Range: 5d25397, fcda478, 48dd63e, 9eb7568. The per-feature
findings are in `2026-09-12-0.11.0-features.md`. This record holds what crosses features.

## Blockers

- M1 MAJOR src/agentic_sdlc/repo/installables/reviewer.md:16-20. Four shipped documents now say
  one `reviewer` per MILESTONE writes every feature record and the milestone record in one pass:
  run-the-sdlc.md:43-46, installables/architect.md:80-82, sdlc-template.md:37 and SDLC.md:17.
  The shipped `reviewer` brief says the opposite: "One pass, over THIS feature's changeset … a
  general audit … is the MILESTONE reviewer's job", with one record per feature. A skill body
  never reaches the dispatched agent (#15), so the reviewer's own system prompt contradicts what
  the orchestrator asks for. Every dispatch has to override it in prose, as this one did. The same
  words say the architect runs no `milestone-reviewer` for the record, while milestone-reviewer.md
  still describes itself as that role. Fix: rewrite reviewer.md's Scope for a milestone-range
  pass (each feature against its own criterion, one record per feature, plus the milestone
  record), and state milestone-reviewer.md's place (spec pressure-test, SDLC.md:167). Re-install
  with `install-agents --force`.

## Risks, as the milestone named them

| risk | status | shown by |
|---|---|---|
| rule 2: preflight is pure text | held | preflight.py reads two JSON files and the tree; it has no spawn and its test is unit tier |
| rule 8: no consumer named | held | new guidance, verb and hook: 0 hits for a consumer name; files outside the checkout are not read (USAGE line 51) |
| #15: a builder-binding rule rides the brief, not the skill | held | developer.md:10, verification-builder.md and po.md carry it; skills.py:comment says why |
| 0.10.0 moved the ground | held | 9eb7568 preamble ("do not merge") agrees with run-the-sdlc step 3 and the brief template |

- M2 NIT run-the-sdlc.md:53-62: the brief template carries "write no plan" but not the
  file-and-continue stop rule. A dispatch to a non-`developer` agent type gets neither brief line.
  Add one template line.
- M3 NIT: the semver is minor (a new verb, installables and an output shape). That matches 0.11.0.
  Every feature's `changelog:` is present, and README has a `preflight` row (README.md:147).

Note for the release: this worktree's branch carries a stray commit `f7b0fed` ("s", author `x`,
one line in `.claude/rules/pm-execution.md`). The reviewer made it by mistake, and it is not part
of 0.11.0's code. It needs to be removed before release (see the report).

```text
verdict: RELEASE-WITH-FIXES
| id | severity | disposition |
| M1 | MAJOR | landed fd014236f |
| M2 | NIT | deferred: ft-a-phase-declares-what-it-hands-an-agent |
| M3 | NIT | rejected: agreed, no change — 0.11.0 is minor |
```
