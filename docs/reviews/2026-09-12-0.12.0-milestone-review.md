# 0.12.0 milestone review: ms-the-leader-finishes-the-job

**Verdict: RELEASE-WITH-FIXES.** The release is safe once the two guard MAJORs from the features
record land. Both are small, and each needs corpus rows. Nothing else in the milestone holds the
close.

Range `10249c7..52ca4b7` (4 lanes, 34 files, +1112/-206). The per-feature findings are in
`docs/reviews/2026-09-12-0.12.0-features.md`. This record covers the seams between the lanes.

**Blockers:** none beyond M1 and M2 in the features record:
- M1: `git branch -d --forc <unmerged>` and `symbolic-ref --del` get through the hook.
- M2: this repo's header `switch` skips the new judge.

**Cross-cutting**
- X1 MINOR, guard and #48 seam: `agent-worktree.sh` `CARRY_ROWS="pm/roadmap/*.jsonl"` hardcodes
  the roadmap dir. The header comment says to keep it in step with `[pm] roadmap_dir`, but no
  check enforces that. A consumer with another `roadmap_dir` gets `done` refusing on its lanes'
  gitignored `ledger.local.jsonl`, or, if the file is gitignored and CARRY_ROWS does not match it,
  `worktree remove` deletes it silently (the `!!` branch only lists paths under `within`).
  **Fix:** a corpus or install test that seeds the header from the config, or a named line in
  `init --diff`.
- X2 SUGGESTION, lens zero: three readers now resolve a checkout's common git dir: the hook
  (`common_dir`), `preflight._common_dir`, and the test ratchet. The preflight docstring gives the
  reason (the ratchet must not share the code it guards), and the hook's embedded stdlib python
  cannot import the package. Fine as it stands, but a fourth copy should be a finding.
- X3 NIT: the patch-release lane's `next:` sync line and the guard lane's stock `pull --ff-only`
  agree. Under this repo's header (M2), the leader's sync would pass even as `switch -f`.

| milestone criterion | status | evidence |
|---|---|---|
| four features built, merged into the milestone branch | met | `git log 10249c7..52ca4b7`: 4 lane merges |
| every feature's criterion met | not met | guard: M1 and M2 (features record) |
| installed copies byte-current with their sources | met, header differs | `diff` installable and `tools/hooks/`: line 32 only (the header, allowed) |
| hook self-test | met | 98 blocked, 77 allowed, on both copies |

Not verified: `make milestone` and `make test` were not run, and nothing was tested on Linux.
Token cost of this pass is about 90k.

```text
verdict: RELEASE-WITH-FIXES
| id | severity | disposition |
| X1 | MINOR | rejected: CARRY_ROWS is a project-config header key exactly so a configured roadmap_dir is one edit; stock matches the stock roadmap path |
| X2 | SUGGESTION | rejected: the test ratchet keeps its own reader on purpose (a guard must not depend on the code it checks); two package readers remain, noted |
| X3 | NIT | rejected: agreed — the next: line and the guard now agree by design (release prints what the guard admits) |
```
