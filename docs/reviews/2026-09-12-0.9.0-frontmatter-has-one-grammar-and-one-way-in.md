# Review — 0.9.0 / ft-frontmatter-has-one-grammar-and-one-way-in

Range `76696cb..1a55d18`, this feature's code is `1a55d18`. One pass, against the feature's ship criterion only.
I ran the probes in scratch copies made with `git archive`, never in this tree.

## Verdict

SHIP-WITH-FIXES: every clause of the criterion is met for `src/`. The census does not cover `tools/dev/`,
which still reaches a private (m1).

## Blockers

None.

## Findings

- **m1 MINOR** `tools/dev/pm_migrate.py:431,434` — this file still reaches `frontmatter._eol`, a name
  listed in `FRONTMATTER_INTERNALS`. Its `_rewritten` (`:255`) is also a second key/list rename grammar,
  and a looser one: it rewrites any `- ` line under any key. `frontmatter.renamed_in` now covers that case
  with the same `renames` mapping. `OneStorage` scans `_sources()`, which is `src/` only
  (`tests/test_boundaries.py:295`), so it prints PASS over this file. This commit edited these same
  functions (`_split` → `split_lines`) and left `_eol` in place. Fix: give `_eol` a public name, or route
  `_with_fields` through the owner. Make `_rewritten` call `renamed_in(text, REF_FIELDS, renames)`. If
  `tools/dev` is out of scope by design, say so beside `_sources()`.
- **N1 NIT** `src/agentic_sdlc/core/frontmatter.py:149` — `_drop_if_written` clears every cached parse on
  any `apply` step, even though the plan knows which paths it wrote and `forget_document` exists. This
  cost nothing measurable on `pm rename` (next row).
- **N2 NIT** `pm/roadmap/stories/st-a-rewritten-document-leaves-the-parse-cache.md:11` — AC2 asks for
  both timings in the close line, and the `done:` line has neither. Measured here, 3 runs each, renaming
  `ft-the-gates-agree-and-a-dispatch-counts-once` over this roadmap: `76696cb` 0.147–0.269 s, `1a55d18`
  0.146–0.162 s.

## Criteria

| criterion | status | evidence |
|---|---|---|
| `pm/rename.py` imports no private from `core/frontmatter.py` and has no list or key grammar of its own | met | `rename.py` has no `frontmatter._` and no `_KEY` regex; `_take` calls `frontmatter.renamed_in` |
| after `pm rename`, a read in the same process sees the new bytes | met | `tests/test_pm_gate.py:160-167`: a plan write with the mtime put back. It fails at `b79c5c8` (probe: `'done' != 'gone'`) and passes at `1a55d18` |
| `test_boundaries` fails a reach, not only a re-binding, with an empty offender list | met for `src/` (m1) | probe: `frontmatter._eol` planted in `grain_shape.py` → `OneStorage` FAILED; the dotted `agentic_sdlc.core.frontmatter._eol` is caught by `TheEngineAsksByIdNotByPath`; `MIN_FRONTMATTER_REACHES = 20` floor |
| `pm rename` over this roadmap takes no more wall-clock than at HEAD | met (measured by review, N2) | timings above. The bytes are also identical between `76696cb` and `1a55d18` for renaming a story, a feature and the milestone (4/5/15 files changed; `diff -r` identical, stdout identical) |

The criterion says "a reach into `FRONTMATTER_INTERNALS`". The code forbids a reach into any `_` private,
and the public internals (`read_raw`, `split_lines`, `fence_bounds`) stay reachable on purpose. That
matches the bug file's intent.

```text
verdict: SHIP-WITH-FIXES
| id | severity | disposition |
| m1 | MINOR | open |
| N1 | NIT | open |
| N2 | NIT | open |
```
