# Review — 0.9.0 ms-the-tool-agrees-with-itself (milestone, cross-cutting)

Range `76696cb..1a55d18`, covering `src tests tools CLAUDE.md` (commits `071b249`, `96515d2`, `b79c5c8`
and `1a55d18`). Each feature's own findings are in its own record. This record covers what the two
features do to each other, plus the milestone criterion.

## Verdict

RELEASE-WITH-FIXES: nothing holds the release. One commit in the range is red on its own (m1). One
consumer-visible change has no changelog line (D1). The GitHub closes are still to do.

## Blockers

None.

## Findings

- **m1 MINOR** `b79c5c8` — this commit is red when run on its own. It carries the other feature's cache
  test (`tests/test_pm_gate.py:160-167`) without the fix, which landed in `1a55d18`. Probe: at `b79c5c8`
  in scratch, `pytest -m "not shell"` gave 1 failed and 1319 passed
  (`OneReadPerDocument::test_a_rewritten_document_is_never_answered_from_the_cache`). So the per-change gate
  for that commit measured a tree that was not the commit, most likely a working tree that also held
  `1a55d18`'s `frontmatter.py`. A bisect over the merge lands on a red commit. Fix: move the hunk into
  `1a55d18` if the branch can still be rewritten. If not, leave it on the record.
- **D1 DELTA** `96515d2` — five shipped installables lower their `effort:`. The builders, po and simplifier
  drop to `medium` and the reviewers to `high`. No grain in this milestone carries a `changelog:` sentence
  for this, so `agentic-sdlc changelog` for 0.9.0 will not tell consumers that their re-installed agents
  changed.

Checked across the two features and found clean: they share `pm/inventory.py` in disjoint hunks, and their
only shared test file is m1's. No behaviour of one reaches the other. The new R5 wording and the `joined`
report line are output-shape changes, which a minor bump (0.9.0) covers under rule 6.

## Criteria

| criterion | status | evidence |
|---|---|---|
| a `start` consumer can release, push and merge without `--no-verify`; R5 and `ci-semver-gate.yml` name one version | met at unit level | R5 now grades the last started milestone. The semver gate wants the PR version to be a `done` milestone's version, greater than main's, and that is the same milestone. Not run end to end in CI |
| a hand record counts once, on its grain, and the report counts the joined pairs | met | feature record, edge in its m1 |
| a grainless row is placed only on exactly one story; a feature-only snapshot lands in `rows naming no grain` | met | feature record |
| `core/frontmatter.py` is the only grammar; `test_boundaries` forbids reaching its privates, with an empty offender list | met for `src/` | `tools/dev/pm_migrate.py` still has a second grammar and an `_eol` reach (feature record m1) |
| `CLAUDE.md` says the hard-rule numbers are append-only | met | `071b249` |
| #43, #39 and #15 closed citing hashes; #40, #41 and #42 commented | not met yet | `gh issue view`: #43, #39 and #15 OPEN at review time. #40–#42 not checked |

```text
verdict: RELEASE-WITH-FIXES
| id | severity | disposition |
| m1 | MINOR | rejected: history is forward-only; each feature close and the release gate run at the branch tip, which is green |
| D1 | DELTA | landed 2ae27e556 |
```
