# 0.13.0 milestone review: ms-a-rung-reads-what-it-reads

**Verdict: RELEASE.** Two merged PRs cut as one release so consumers can pin them. Both were
reviewed on GitHub as #54 and #55 and each carried a green full suite (1732 and 1703 passed) and a
green `make check`.

## ft-a-rung-hashes-what-it-reads (#54)

| criterion | status | evidence |
|---|---|---|
| `[verify.inputs]` scopes a rung's state; absent means the whole tree | met | `tests/test_verify_inputs.py`, `tests/test_verify_spawns.py::AScopedRungReadsOnlyWhatItsTargetReads` |
| a status flip or a doc edit outside the scope reuses the last green | met | `test_a_status_flip_in_the_ledger_reuses_a_scoped_story_rung`, `test_an_edit_outside_the_scope_reuses_and_says_over_what` |
| the graded-rows guard applies to the milestone rung alone | met | `test_a_row_check_budget_grades_landing_since_refuses_the_reuse` retargeted to `--milestone` |
| the scope is in the digest (state tag v3) | met | `test_the_scope_is_in_the_digest` |

## ft-four-agents-install-and-tree-writes-ride-code (#55)

| criterion | status | evidence |
|---|---|---|
| `install-agents` ships four agents | met | `install.PLANS`, `tests/test_install.py` AGENTS |
| the execution rule says tree writes ride code commits, belts once per merge batch | met | `pm/guidance/pm-execution.md`, `tests/test_dispatch.py` |
| the stop hook runs no unit tier when no slice maps | met | `tests/test_hooks_payloads.py::test_stop_gate_names_a_base_that_does_not_resolve` |
| SDLC.md drops the hand-written ledger record | met | `SDLC.md` § The dispatch loop |

Case ceiling 1716 -> 1723: seven cases landed with the two features, all named above.

```text
verdict: RELEASE
| id | severity | disposition |
```
