# Review — 0.14.0 / ft-a-milestone-starts-and-a-lane-counts-once

Range `d25aed2..dc479d0` (`f7816a4` code, `dc479d0` docs). One lane pass, against the feature's ship
criterion only. I ran the probes in a scratch copy made with `git archive`, never in this tree.
`make unit` at `0916cfa`: 1341 passed.

## Verdict

SHIP-WITH-FIXES: the three criteria are met. One MAJOR holds the close: the new `by grain` table
shows a shared row on two features with no `*` mark.

## Blockers

- **M1 MAJOR** `src/agentic_sdlc/repo/pm/report.py:1060` — `grain_rows` marks a grain as shared only
  when it is `not in owned`, so a FEATURE that a lane names directly is never marked. Probe:
  `ledger record --grain "ft-a…,ft-b…" --tokens-total 1000` gives a header of 165911 tokens. The
  `by grain` lines are 165911 and 1000, a sum of 166911, with no `*` and no footnote. A milestone plus
  a feature lane marks only the milestone. The footnote exists to stop this overstatement, and the
  feature is the grain this tree's lanes build. Fix: `if shared and gid in names:`. A feature that
  its stories roll up to stays unmarked, because it is not in `names`. Add one param row with two
  features to the seeded report case.

## Findings

- **m2 MINOR** `src/agentic_sdlc/repo/pm/report.py:594` — `_folded` copies `grains` from the hand
  row only when the hand row has it. So a courier `grains` list stays under a hand-overridden
  `grain`. Probe: `_folded({grain:a, grains:[a,b]}, {grain:d})` returns `grain d, grains [a, b]`. The
  row is owned by `d`, but `by grain` counts it on `a` and `b` and drops `d`. Fix: when the hand
  stamps `grain`, take its `grains` or remove the courier's.
- **N3 NIT** `src/agentic_sdlc/repo/installables/project-devkit.toml:138` — `[pm]
  agent_branch_prefix` and `agent-worktree.sh` `BRANCH_PREFIX` are two declarations of one prefix.
  Only a comment says to change both together.
- **N4 NIT** — the builder's gap "no test covers `ledger show` reading `grains`": I probed it, and
  `ledger show ft-a-surface-says-what-it-means` lists the lane row (`row_names` goes through
  `grains_of`). The courier comma list goes through the same `_lane_of` call (`cli.py:2634`). I read
  that path but did not run it with a transcript. Neither case has a test.

Not verified: that the #52 and #59 tests fail at `d25aed2`, and `make check`.

## Criteria

| criterion | status | evidence |
|---|---|---|
| `pm set <ms> branch feat/x` exits 1 and names `milestone/…` | met | scratch probe: rc=1, "Use `milestone/0.14.0-the-filed-issues-are-answered`", file byte-unchanged. `ready`→`building` on `feat/x` gives rc=1. `agent_branch_prefix = 3` gives rc=2. `""` refuses nothing. `tests/test_pm_verbs.py::test_a_milestone_branch_under_the_agent_prefix_is_refused_at_both_writes` |
| one `ledger record` over three grains counts once in `ledger report` totals | met | `tests/test_pm_ledger_report.py` TABLE: 5 units, 13500 tokens. Probe header: 164911 + 1000 = 165911, 2 units. `a,,b`, `,` and `a,a` exit 2 with no row written |
| the installed skill and reviewer describe per-lane review and a milestone checkup | met | `.claude/skills/run-the-sdlc/SKILL.md` and `.claude/agents/reviewer.md` match their sources below the header. `tests/test_pm_guidance.py`. `ready-for` resolves records by pointer (`ready_for.py:528`), so records from separate passes are accepted |

```text
verdict: SHIP-WITH-FIXES
| id | severity | disposition |
| M1 | MAJOR | landed 62425e5 |
| m2 | MINOR | landed 18789eb |
| N3 | NIT | rejected: the shell script cannot read devkit.toml; the seed comment names both sites |
| N4 | NIT | rejected: `ledger show` and the courier read `grains` through the same `grains_of` the report test covers |
```
