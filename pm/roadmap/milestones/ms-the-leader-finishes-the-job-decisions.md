Append with `make pm ARGS='decide <grain-id>'` — never by hand; the command stamps the date and the next ordinal.

# ms-the-leader-finishes-the-job the leader finishes the job — decisions

Durable. This log outlives the grain: it is where a choice and its rejected
alternative are recorded, and it survives close.

> Never write what is derivable. `pm status` gives tallies, `git log` gives
> history. This file holds the WHY that neither of them records.

## D1 — 2026-09-12 — this repo's guard header does not carry switch — stock allows the safe forms

Reverses the feature brief's "the header keeps `switch`". A header entry allows a subcommand with ANY arguments, so it bypassed the stock rules this milestone added (`switch --discard-changes`, `-f`, `-C` passed here while stock refused them; review M2). The only reason for the entry was the leader's own `git switch <branch>`, which stock now allows. **Rejected:** keeping the entry for convenience; parallel agents share this checkout, and discarding each other's edits is the loss the guard exists to stop. Landed b22bcf7.
